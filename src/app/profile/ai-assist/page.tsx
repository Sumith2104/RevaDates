import { AppShell } from '@/components/shared/app-shell';
import { AiAssistView } from '@/components/profile/ai-assist-view';

export default function AiAssistPage() {
  return (
    <AppShell>
      <AiAssistView />
    </AppShell>
  );
}
